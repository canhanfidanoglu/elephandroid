# ── Required ────────────────────────────────────────────────────────

subscription_id         = "00000000-0000-0000-0000-000000000000"
azure_client_id         = "your-azure-ad-client-id"
azure_client_secret     = "your-azure-ad-client-secret"
session_secret_key      = "generate-a-random-secret-key"
postgres_admin_password = "StrongPassword123!"

# ── Optional (defaults shown) ──────────────────────────────────────

# project_name       = "elephandroid"
# location           = "westeurope"
# environment        = "production"
# api_image_tag      = "latest"
# frontend_image_tag = "latest"

# ── LLM Provider ───────────────────────────────────────────────────

# llm_provider     = "claude"
# anthropic_api_key = "sk-ant-..."
# openai_api_key   = "sk-..."
# gemini_api_key   = "AI..."

# ── Bot Service (optional) ─────────────────────────────────────────

# bot_app_id       = "your-bot-app-id"
# bot_app_password = "your-bot-app-password"

# ── CORS ───────────────────────────────────────────────────────────

# cors_origins = "[\"https://yourdomain.com\"]"
